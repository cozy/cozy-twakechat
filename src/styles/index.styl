@require 'settings/breakpoints.styl'

// Disable vertical overscroll behavior (bouncing) because it is too aggressive when scrolling with the trackpad on MacOS
:root
    overscroll-behavior-y none
    overflow-y hidden

body
    display flex
    height 100vh

iframe
    +medium-screen()
        height calc(100% - 3rem)
        padding-top 3rem
